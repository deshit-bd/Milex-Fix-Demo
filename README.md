# MileX App

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:3000

The frontend and backend API routes run together through Next.js. Google Sheets storage is optional; copy `.env.example` to `.env.local` and fill values only when you want live Sheet persistence.
